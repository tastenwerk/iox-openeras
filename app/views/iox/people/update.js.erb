<% if flash.notice %>
  if( typeof(reloadPreview) === 'function' )
    reloadPreview();
  if( $('.iox-win:visible').length )
    iox.Win.closeVisible();
  $.unblockUI();
  $('#person-grid').data('kendoGrid').dataSource.read();
<% else %>
  $('ul.anchor a[href="#step1"]').click();
<% end %>
iox.flash.rails( <%= raw flash.to_json %> );