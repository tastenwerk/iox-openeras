<% if flash.notice %>
  //$('#program-entry-wizard').smartWizard('setError', {stepnum:0,iserror:false});
  if( typeof(reloadPreview) === 'function' )
    reloadPreview();
  $('#pe-grid').data('kendoGrid').dataSource.read()
<% else %>
  //$('#program-entry-wizard').smartWizard('setError', {stepnum:0,iserror:true});
  $('ul.anchor a[href="#step1"]').click();
<% end %>
iox.flash.rails( <%= raw flash.to_json %> );
$('.stepContainer').unblock();
iox.Win.closeAll();